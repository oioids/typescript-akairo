# typescript-akairo

### Create a file named Config.ts and paste this, then fill it out

export let token: string = "token";

export let prefix: string = "prefix";

export let owners: string[] = ["owner ids"];

export let color: string = "hex color";

After that, run tsc and cd into the dist folder. Then node bot.js/
